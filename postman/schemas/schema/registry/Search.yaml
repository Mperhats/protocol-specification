description: A query to find nearby Seller Supporting Nodes (SSN)
type: object
properties:
  location:
    description: A location defining where the services or products are to be fulfilled 
    $ref: '../shared/primitives/Coordinate.yaml'
  codes: 
    description: Defines a filter for a specific provider network code or codes
    type: string
    items:
      $ref: '../shared/base_types/ProviderType.yaml#/properties/code'
required:
  - location