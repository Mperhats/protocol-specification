title: Search
description: Requied search parameters for a Buyer Supporting Node (BSN) to query a Seller Supporting Node (SSN)
type: object
properties:
  context:
    $ref: '../schema/shared/Context.yaml'
  intent:
    $ref: '../schema/merchant/SearchIntent.yaml'
required:
  - context
  - intent